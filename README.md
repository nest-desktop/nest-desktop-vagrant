# NEST Desktop with vagrant

### Installation

Install vagrant (See https://developer.hashicorp.com/vagrant/downloads)

### Usage

Install and start VM instance:
```
vagrant up
```

Reload VM instance:
```
vagrant reload
```

Stop VM instance:
```
vagrant halt
```